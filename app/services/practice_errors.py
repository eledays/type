class PracticeError(ValueError):
    """Stable domain error returned by the practice API."""

    def __init__(self, code: str, message: str, status: int = 400) -> None:
        super().__init__(message)
        self.code = code
        self.message = message
        self.status = status
