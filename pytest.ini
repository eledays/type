[pytest]
testpaths = tests
addopts = -ra
filterwarnings =
    error:datetime\.datetime\.utcnow\(\) is deprecated.*:DeprecationWarning:flask_login\.login_manager
markers =
    postgres: integration tests that require a dedicated PostgreSQL database
